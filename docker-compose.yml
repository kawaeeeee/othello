version: '3.8'
services:
  app:
    build:
      context: ./
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ./:/workspace:cached
    ports:
      - 9000:8000 
      - 3000:3000 
    command: sleep infinity
    environment:
      PYTHONPATH: /workspace/backend/src